# Agent Interface for http://127.0.0.1:33235

Auto-generated by Agent-See. This MCP server wraps the original site
and exposes 14 capabilities as structured tools.

## Operational Posture

This package separates **interface generation** from **operational hardening**.
It ships with:

- grounded route mappings where available
- generated browser scaffolding for UI-discovered actions
- explicit approval gates for sensitive operations
- starter-grade runtime state files for session-aware workflows

It does **not** claim that every tool is already live-verified against production drift.
Use `tool_metadata.json`, `runtime_state.json`, and `route_map.json` during hardening.

## Quick Start

```bash
cp .env.example .env
# Edit .env with TARGET_URL and API credentials
uv run server.py
```

## One-Click Deploy

```bash
# Fly.io
flyctl deploy

# Railway
railway up

# Docker Compose
docker compose up --build -d

# Or use the helper script
./deploy.sh
```

## Tools

- **list_products**: List all products in the bakery catalog (backend: `api`, readiness: `route_mapped`)
- **get_product_details**: Get full details for a specific product (backend: `api`, readiness: `route_mapped`)
- **get_cart**: Get current shopping cart contents (backend: `api`, readiness: `route_mapped`)
- **add_to_cart**: Add a product to the shopping cart (backend: `api`, readiness: `route_mapped`)
- **submit_checkout**: Initiate checkout - returns payment URL for confirmation (backend: `api`, readiness: `route_mapped`)
- **get_order_status**: Get order status and tracking info (backend: `api`, readiness: `route_mapped`)
- **send_message**: Send a message to the business (backend: `browser`, readiness: `generated_browser`)
- **search_items**: Search for items on the site (backend: `browser`, readiness: `generated_browser`)
- **get_business_info**: Get business contact information, hours, and location (backend: `browser`, readiness: `generated_browser`)
- **check_inventory**: Check real-time stock status for a product variant (backend: `hybrid`, readiness: `structural_only`)
- **remove_from_cart**: Remove a product from the shopping cart (backend: `hybrid`, readiness: `structural_only`)
- **apply_coupon**: Apply a coupon or discount code to the cart (backend: `hybrid`, readiness: `structural_only`)
- **calculate_shipping**: Calculate shipping options and costs for delivery address (backend: `hybrid`, readiness: `structural_only`)
- **initiate_return**: Start a return or refund request for an order (backend: `hybrid`, readiness: `structural_only`)

## Runtime Files

- `route_map.json`: grounded API route mapping used by API-backed tools
- `tool_metadata.json`: readiness, approval, and verification metadata per tool
- `runtime_state.json`: inferred session and workflow state scaffolding
